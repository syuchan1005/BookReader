query readList($revisionCount: Int) {
    readList(beforeRevisionCount: $revisionCount) {
        latestRevision {
            count
            syncedAt
        }
        readList {
            infoId
            bookId
            page
            updatedAt
        }
    }
}
