# Pages_Info
# Hooks_usePrevNextBook
# SelectBookInfoThumbnailDialog
query bookInfo($id: ID!){
    bookInfo(id: $id) {
        id
        name
        books {
            id
            number
            pages
            thumbnail

            info {
                id
            }
        }
    }
}
